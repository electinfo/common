{
  "name": "@electinfo/common",
  "version": "0.2.1",
  "description": "Shared schemas, templates, and constants for electinfo",
  "main": "dist/src/main/typescript/index.js",
  "types": "dist/src/main/typescript/index.d.ts",
  "files": [
    "dist",
    "schemas",
    "templates"
  ],
  "scripts": {
    "yaml2json": "node scripts/yaml2json.js",
    "generate:scala": "node scripts/generate-model.js",
    "generate:scala:check": "node scripts/generate-model.js --check",
    "build": "npm run yaml2json && npm run generate:scala && tsc",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/electinfo/common.git"
  },
  "author": "electinfo",
  "license": "MIT",
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "typescript": "^5.0.0"
  }
}